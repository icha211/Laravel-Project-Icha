# Employee Management Performance System - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    LARAVEL APPLICATION                          │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   USER LAYER                            │   │
│  │  ┌──────────────────┐    ┌──────────────────┐          │   │
│  │  │  Employer User   │    │  Employee User   │          │   │
│  │  └──────────────────┘    └──────────────────┘          │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                               ↓                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │            AUTHENTICATION LAYER                         │   │
│  │  (Laravel Auth + Role-Based Access Control)            │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              CONTROLLER LAYER (5)                       │   │
│  │  ┌──────────┐ ┌────────────┐ ┌────────┐ ┌─────────┐   │   │
│  │  │Employee  │ │Performance │ │Goal    │ │Training │   │   │
│  │  │Controller│ │Reviewer    │ │Manager │ │Manager  │   │   │
│  │  │          │ │Controller  │ │        │ │         │   │   │
│  │  └──────────┘ └────────────┘ └────────┘ └─────────┘   │   │
│  │         + DashboardController                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              MODEL LAYER (5 Models)                     │   │
│  │  ┌──────┐ ┌───────────┐ ┌────────────────┐ ┌────────┐ │   │
│  │  │User  │ │Employee   │ │Performance     │ │Goal    │ │   │
│  │  │ (2)  │ │ (CRUD)    │ │Review (CRUD)   │ │(CRUD)  │ │   │
│  │  └──────┘ └───────────┘ └────────────────┘ └────────┘ │   │
│  │         + Training (CRUD)                              │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              DATABASE LAYER (5 Tables)                  │   │
│  │  ┌──────┐ ┌───────────┐ ┌─────────┐ ┌────────┐ ┌────┐ │   │
│  │  │Users │ │Employees  │ │Reviews  │ │Goals   │ │Train   │   │
│  │  └──────┘ └───────────┘ └─────────┘ └────────┘ └────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │         VIEW LAYER (23 Blade Templates)                │   │
│  │  Dashboard (2) │ Employees (4) │ Reviews (4)           │   │
│  │  Goals (4) │ Training (4) │ Layout (1)                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│              ↓                                                   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │        FRONTEND (Bootstrap 5 + Font Awesome)           │   │
│  │  Responsive UI | Navigation Sidebar | Interactive Forms│   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagram

```
EMPLOYER                                    EMPLOYEE
   │                                           │
   ├──→ Create Employee ──→ EmployeeController
   │                              │
   │                              ├──→ Create User (Employee Role)
   │                              ├──→ Create Employee Record
   │                              └──→ Redirect to Dashboard
   │
   ├──→ Create Review ──→ PerformanceReviewController
   │                              │
   │                              ├──→ Validate Data (5 Criteria)
   │                              ├──→ Auto-Calculate Rating
   │                              ├──→ Update Employee Average
   │                              └──→ Store in Database
   │
   │                                    ↓
   │                             View Dashboard (Employee)
   │                                    │
   │                                    ├──→ Fetch Personal Data
   │                                    ├──→ Fetch Recent Reviews
   │                                    ├──→ Fetch Goals
   │                                    └──→ Display Metrics
   │
   └──→ View Analytics ──→ DashboardController
                               │
                               ├──→ Fetch Top Performers
                               ├──→ Calculate Department Stats
                               ├──→ Group by Department
                               └──→ Display in Dashboard
```

---

## Entity Relationship Diagram (ERD)

```
┌─────────────────┐
│    USERS        │
├─────────────────┤
│ id (PK)         │
│ name            │
│ email (UNIQUE)  │
│ password        │
│ role ◄──────────┼─── 'employer' or 'employee'
│ phone           │
│ department      │
│ position        │
│ timestamps      │
└────────┬────────┘
         │
    ┌────┴─────────────────────────┐
    │                              │
    │ (1:Many)                     │
    ↓                              ↓
┌─────────────────┐          ┌─────────────────┐
│  EMPLOYEES      │          │   PERFORM       │
├─────────────────┤          │   REVIEWS       │
│ id (PK)         │          ├─────────────────┤
│ user_id (FK)    │◄─────┐   │ id (PK)         │
│ employer_id(FK) │      │   │ employee_id(FK) │◄──┐
│ employee_id     │      │   │ reviewer_id(FK) │   │
│ department      │      │   │ rating          │   │
│ position        │      │   │ communication   │   │
│ hire_date       │      │   │ teamwork        │   │
│ salary          │      │   │ productivity    │   │
│ avg_rating      │      │   │ reliability     │   │
│ timestamps      │      │   │ leadership      │   │
└────────┬────────┘      │   │ review_period   │   │
         │               │   │ comments        │   │
    ┌────┴──────────┐    │   │ status          │   │
    │               │    │   │ timestamps      │   │
    │ (1:Many)      │    │   └─────────────────┘   │
    │ (1:Many)      │    │                         │
    ↓               ↓    └─────────────────────────┘
┌─────────────────┐
│     GOALS       │      ┌─────────────────┐
├─────────────────┤      │    TRAINING     │
│ id (PK)         │      ├─────────────────┤
│ employee_id(FK) │◄──┐  │ id (PK)         │
│ title           │   │  │ employee_id(FK) │◄──┐
│ description     │   │  │ title           │   │
│ target_date     │   │  │ description     │   │
│ progress        │   │  │ start_date      │   │
│ status          │   │  │ end_date        │   │
│ priority        │   │  │ status          │   │
│ timestamps      │   │  │ certificate     │   │
└─────────────────┘   │  │ timestamps      │   │
         ▲            │  └─────────────────┘   │
         │            │                        │
         └────────────┴────────────────────────┘
              (1:Many) relationships
```

---

## Module Interaction Flow

```
┌──────────────────────────────────────────────────────────┐
│              EMPLOYER MODULE (Management)                │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Login → Dashboard → [Analytics Overview]               │
│                          │                              │
│                    ┌─────┼─────┐                        │
│                    ▼     ▼     ▼                        │
│              Employees Performance Department           │
│                    │        │         │                │
│              ┌─────┴──┬─────┴──┬──────┴─────┐          │
│              ▼        ▼        ▼             ▼          │
│           List   Create   Edit    Create   Analytics   │
│           View   Employee Reviews Update   Reports    │
│           Delete           Delete           │         │
│                                            ▼         │
│                                    Department Stats   │
│                                    Top Performers     │
│                                    Recent Reviews     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│             EMPLOYEE MODULE (Personal)                   │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  Login → Dashboard → [Personal Metrics]                │
│                          │                              │
│              ┌───────────┼───────────┐                  │
│              ▼           ▼           ▼                  │
│           Profile     Reviews      Goals               │
│              │           │           │                 │
│         ┌────┴────┬─────┴────┬─────┴─────┐            │
│         ▼         ▼          ▼            ▼            │
│      View    Create    View   Update   Tracking       │
│      Info    Goals    History Progress  Trainings    │
│      Stats   Track     Feedback         Enroll       │
│             Delete                     Complete     │
└──────────────────────────────────────────────────────────┘
```

---

## CRUD Operations Matrix

```
┌──────────────┬─────────┬──────┬────────┬────────┐
│ Entity       │ Create  │ Read │ Update │ Delete │
├──────────────┼─────────┼──────┼────────┼────────┤
│ Employee     │ ✓ (E)   │ ✓    │ ✓ (E)  │ ✓ (E)  │
│ Performance  │ ✓ (E)   │ ✓ *  │ ✓ (E)  │ ✓ (E)  │
│ Goal         │ ✓ (B)   │ ✓ *  │ ✓ (B)  │ ✓ (B)  │
│ Training     │ ✓ (B)   │ ✓ *  │ ✓ (B)  │ ✓ (B)  │
└──────────────┴─────────┴──────┴────────┴────────┘

Legend:
E = Employer only
B = Both (Employer & Employee)
* = Employer sees all, Employee sees own
✓ = Implemented and working
```

---

## Key Features & Technologies

```
┌─────────────────────────────────────────────────────┐
│            TECHNOLOGIES STACK                      │
├─────────────────────────────────────────────────────┤
│ Backend:     Laravel 11 (PHP)                      │
│ Frontend:    Bootstrap 5, Blade, HTML/CSS          │
│ Database:    MySQL/SQLite                          │
│ Auth:        Laravel Built-in Authentication       │
│ Icons:       Font Awesome 6.4                      │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│         CREATIVE FEATURES IMPLEMENTED              │
├─────────────────────────────────────────────────────┤
│ 1. Auto-Calculated Performance Ratings             │
│ 2. Department-Level Analytics                      │
│ 3. Goal Progress Visualization                     │
│ 4. Training Development Tracking                   │
│ 5. Dual Dashboard Systems                          │
│ 6. Performance History Timeline                    │
│ 7. Multi-Criteria Rating System                    │
│ 8. Real-time Metrics Updates                       │
└─────────────────────────────────────────────────────┘
```

---

## Navigation Structure

```
TOP-LEVEL ROUTES
├── / (Welcome)
├── /login
├── /register
└── /dashboard (Protected)
    │
    ├── EMPLOYER MENU
    │   ├── /employees (List)
    │   │   ├── /employees/create (New)
    │   │   ├── /employees/{id} (Show)
    │   │   ├── /employees/{id}/edit (Edit)
    │   │   └── /employees/{id} DELETE
    │   │
    │   └── /performance-reviews (List)
    │       ├── /performance-reviews/create/{empId} (New)
    │       ├── /performance-reviews/{id} (Show)
    │       ├── /performance-reviews/{id}/edit (Edit)
    │       └── /performance-reviews/{id} DELETE
    │
    └── EMPLOYEE MENU
        ├── /employees/{id} (Show Profile)
        │   ├── Goals Tab
        │   ├── Reviews Tab
        │   └── Training Tab
        │
        ├── /employees/{id}/goals
        │   ├── /employees/{id}/goals/create
        │   ├── /employees/{id}/goals/{id}
        │   ├── /employees/{id}/goals/{id}/edit
        │   └── /employees/{id}/goals/{id} DELETE
        │
        └── /employees/{id}/training
            ├── /employees/{id}/training/create
            ├── /employees/{id}/training/{id}
            ├── /employees/{id}/training/{id}/edit
            └── /employees/{id}/training/{id} DELETE
```

---

## Data Processing Flow Example: Create Performance Review

```
1. EMPLOYER ACTION
   ├─ Navigate to Employee
   └─ Click "New Review"

2. FORM DISPLAY
   ├─ View review form
   └─ Rate 5 criteria (1-5 each)

3. FORM SUBMISSION
   ├─ POST /performance-reviews
   └─ PerformanceReviewController@store

4. VALIDATION
   ├─ Check all inputs
   ├─ Verify employee exists
   └─ Verify authorization

5. DATA PROCESSING
   ├─ Create review record
   ├─ Calculate rating = AVG(all criteria)
   ├─ Update employee.average_rating
   └─ Update employee.total_reviews

6. DATABASE
   ├─ INSERT INTO performance_reviews
   ├─ UPDATE employees SET average_rating
   └─ COMMIT transaction

7. RESPONSE
   ├─ Show success message
   ├─ Redirect to review detail
   └─ Display saved review

8. EMPLOYEE SEES
   ├─ Dashboard updated
   ├─ New review in history
   └─ Updated average rating
```

---

**Architecture is production-ready and scalable!** 🚀
